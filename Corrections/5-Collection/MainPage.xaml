<Page
    x:Class="_5_Collection.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:_5_Collection"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    xmlns:vm="using:_5_Collection.ViewModels">

    <Page.DataContext>
        <vm:MainPageViewModel />
    </Page.DataContext>
    
    <Grid>
        <ListView ItemsSource="{Binding Restaurants}" 
                  ItemClick="GoToDetail_Click"
                  IsItemClickEnabled="True">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                        <StackPanel Width="75" Height="75">
                            <Image Source="{Binding PictureUri}" MaxHeight="75" Stretch="None" />
                        </StackPanel>
                        <StackPanel>
                            <TextBlock Text="{Binding Name}" />
                        </StackPanel>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
